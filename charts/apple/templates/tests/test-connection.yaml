apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apple.fullname" . }}-test-connection"
  labels:
    {{- include "apple.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apple.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
